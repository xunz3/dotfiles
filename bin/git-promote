#!/bin/sh
#
# Promotes a local topic branch to a remote tracking branch of the same name,
# by pushing it with an upstream in one operation.

set -eu

branch=$(git branch --show-current)
if [ -z "$branch" ]; then
  echo "git-promote: HEAD is detached" >&2
  exit 1
fi

exec git push --set-upstream origin "$branch"
